server:
  port: 8080
  
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      order_inserts: true
      dialect: org.hibernate.dialect.PostgreSQLDialect
      
  datasource:
    url: jdbc:postgresql://localhost:5432/tourofheroes9506
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Juan9506